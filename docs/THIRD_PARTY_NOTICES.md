# Third-party and provenance notes

The workspace includes the `serial` package under `src/third_party/serial`, whose package metadata declares Apache License 2.0.

The S200-compatible serial frame formats and scaling in `inspection_robot_base` were derived from the supplied reference robot source. The reference package did not provide a clear final license declaration in the reviewed copy. Renaming code does not resolve provenance or redistribution rights. Before publishing or commercial distribution, obtain/verify permission for any derived implementation and preserve required notices.
